// The documentation site is not part of the goque Go module. This file exists
// so the go tool treats website/ as a separate module and never descends into
// node_modules, where some npm packages ship their own .go sources.
module github.com/swissy-dev/goque-website

go 1.24
